git add path
 - adds edited files to staged state so they are ready to be added to git history

git commit -m "description"
 - commits a snapshot to githistory with 3 things: 1) Information about changes 2) Reference to parent commit 3) Hash code name

git push
 - adds your local changes to the remote repository